# Grafana server config (bind to Tailscale IP, not localhost).
GF_SERVER_HTTP_ADDR=100.116.249.72
GF_SERVER_HTTP_PORT=3000

# Provisioning paths (use repo path for local dev or /etc/grafana/provisioning on hosts).
GF_PATHS_PROVISIONING=/etc/grafana/provisioning
PROVISIONING_CFG_DIR=/etc/grafana/provisioning

# TimescaleDB datasource config for provisioning.
GF_TIMESCALE_HOST=127.0.0.1
GF_TIMESCALE_PORT=5432
GF_TIMESCALE_DB=hl_carry
GF_TIMESCALE_USER=hlbot
GF_TIMESCALE_PASSWORD=secret
GF_TIMESCALE_SSLMODE=disable
