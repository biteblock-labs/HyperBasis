apiVersion: 1

providers:
  - name: hl-carry-bot
    orgId: 1
    folder: hl-carry-bot
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: ${PROVISIONING_CFG_DIR}/dashboards/hl-carry-bot
